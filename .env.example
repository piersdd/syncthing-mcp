# ── Syncthing MCP — environment variables ──────────────────────────
#
# Copy this file to .env and fill in real values before deployment.

# Domain used by Traefik for the ACME certificate and Host() rule.
DOMAIN=example.com

# Bearer token that clients must send as "Authorization: Bearer <token>".
# Generate one with:  openssl rand -hex 32
MCP_AUTH_TOKEN=CHANGEME

# ── Syncthing instances ─────────────────────────────────────────────
# JSON object keyed by friendly name.  Each entry needs url + api_key.
#
# NOTE: From inside the Docker network, use the Docker hostname or
#       host-gateway IP for instances running on the same machine.
#       For remote instances, use their LAN / Tailscale / WireGuard address.
SYNCTHING_INSTANCES='{
  "mini":   {"url": "http://mini.local:8384",   "api_key": "MINI_API_KEY"},
  "tn-sb":  {"url": "http://host.docker.internal:8384", "api_key": "SB_API_KEY"},
  "tn-mr2": {"url": "http://tn-mr2.local:8384", "api_key": "MR2_API_KEY"},
  "p-mba":  {"url": "http://p-mba.local:8384",  "api_key": "MBA_API_KEY"}
}'
